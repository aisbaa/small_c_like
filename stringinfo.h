/* stringfunctions
 *
 * Contains string info functions
 */

#ifndef STING_INFO_HEADER
#define STING_INFO_HEADER

/*
 * These four functions works wit ascii only
 */

bool isDecimalDigit(char);

bool isLetter(char);

bool isSpecialCharacter(char);

bool isWhiteSpace(char);

#endif
